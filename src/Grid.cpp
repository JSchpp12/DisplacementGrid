#include "Grid.hpp"

std::vector<std::unique_ptr<star::StarMesh>> Grid::loadMeshes()
{
	for (int i = 0; i < numVertsX; i++) {
		for (int j = 0; j < numVertsY; j++) {

		}
	}

	return std::vector<std::unique_ptr<star::StarMesh>>();
}
